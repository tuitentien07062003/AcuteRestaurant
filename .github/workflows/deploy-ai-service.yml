name: CI/CD Docker for AI Service

on:
  push:
    branches: [ "main" ]
    paths:
      - 'ai_service/**'
      - '.github/workflows/deploy-ai-service.yml'

jobs:
  docker-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t ai-service-image ./ai_service

      - name: Run container
        run: |
          docker run -d -p 8000:8000 --name test-container ai-service-image
          echo "Đang đợi AI load model vào RAM..."
          sleep 30

      - name: Health check
        run: |
          if ! curl -f http://localhost:8000/health; then
            echo "--- DOCKER LOGS TRONG CONTAINER ---"
            docker logs test-container
            echo "-----------------------------------"
            exit 1
          fi

      - name: Stop container
        run: docker rm -f test-container

      - name: Trigger Render Deploy
        if: success()
        run: curl -f -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL_AI_SERVICE }}"